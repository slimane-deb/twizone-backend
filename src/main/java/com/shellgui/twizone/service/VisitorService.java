// TODO For later
/*
package com.own.expertfinder.service;

import com.own.expertfinder.model.Visitor;
import com.own.expertfinder.repository.VisitorRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

import java.util.List;

@Component
public class VisitorService {

    @Autowired
    private VisitorRepository visitorRepository;

    public List<Visitor> getAll() {
        return visitorRepository.findAll();
    }

    public Visitor getOne(Integer id) {
        return visitorRepository.getOne(id);
    }

    public Visitor getOneByUserId(Integer userId) {
        return visitorRepository.findByUserId(userId);
    }

}

 */